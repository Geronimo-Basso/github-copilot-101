# Orchestration Entry — Book

- **Timestamp (UTC):** 2026-04-20T10:02:26Z
- **Agent:** Book (Copilot Expert)
- **Mode:** background
- **Model:** claude-opus-4.7
- **Topic:** Capability inventory of GitHub Copilot Agent Mode (VS Code)
  as authoritative scoping input for Lab 3.

## Task

Produce a sourced inventory of what Agent Mode is, what's stable vs
preview, what has recently been renamed, common pitfalls, and a
ranked Lab 3 scope recommendation.

## Outcome

✅ Success. Deliverable: `.squad/decisions/inbox/book-agent-mode-inventory.md`.
All claims cited to `code.visualstudio.com/docs` URLs accessed
2026-04-20.

## Key moves

- **Corrected two factual errors that would have shipped in the lab:**
  1. **"Plan = renamed Edit" is wrong.** Edit mode is *deprecated*;
     Plan is a separate built-in agent persona. Lab opener must
     retract this framing.
  2. **Custom agents live in `.github/agents/*.agent.md`**, not
     `.github/chatmodes/*.chatmode.md`. The "custom chat modes" name
     is the old one; both Zoe's plan and River's design used the
     stale path.
- Mapped current agent picker hierarchy: **Target (Local/CLI/Cloud/3P)
  × Agent (Agent/Plan/Ask/custom) × Permission level (Default /
  Bypass / Autopilot)**.
- Inventoried 10 stable capabilities + 7 preview/experimental items
  + 5 recent renames + 10 common pitfalls.
- Recommended Lab 3 scope (4 picks, ranked): agent loop in practice,
  permission levels, MCP integration, custom agents with Plan→Impl
  handoff. Explicitly **deferred cloud agents and CLI background
  agents** to a future lab (conflicts with Zoe's 3-layer plan —
  Kaylee must adjudicate).

## Open items / contradictions surfaced

- **vs Zoe:** Book recommends *deferring* cloud Coding Agent; Zoe
  builds the lab around it as the "payoff." Needs reconciliation.
- **vs River:** River's chatmode spec uses old syntax
  (`.chatmode.md`, `tools:` frontmatter array). Book confirms new
  syntax (`.agent.md`, expanded frontmatter incl. `agents`,
  `handoffs`, `target`, `user-invocable`).

## Notes

- This document is the source-of-truth reference Kaylee should cite
  when writing the lab. Zoe's and River's docs supply the *shape*;
  Book's supplies the *facts*.
