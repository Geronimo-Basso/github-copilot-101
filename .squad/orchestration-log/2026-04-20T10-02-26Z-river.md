# Orchestration Entry — River

- **Timestamp (UTC):** 2026-04-20T10:02:26Z
- **Agent:** River (Patterns & Prompting)
- **Mode:** background
- **Model:** claude-opus-4.7
- **Topic:** Hands-on design for Lab 3 — exercises, custom-agent spec,
  prompting patterns, anti-patterns.

## Task

Read the `agents/` codebase, identify the improvement backlog, design
3 cumulative hands-on exercises filling ~55 min, draft a concrete
custom-chatmode spec, and document agent-mode-specific prompting
patterns + anti-patterns.

## Outcome

✅ Success. Deliverable: `.squad/decisions/inbox/river-lab3-handson-design.md`.

## Key moves

- **App-state assessment.** Read `agents/backend/app.py`,
  `frontend/`, `requirements.txt`, `data/activities.json`. Logged a
  10-item improvement backlog (orphan JSON file, no duplicate-signup
  guard, no capacity check, no unregister, no tests, weak email
  validation, query-string vs JSON body, no filter/category, no
  persistence, no frontend error handling).
- **Designed 3 cumulative exercises** (~55 min total):
  1. **Multi-file bug fix + test scaffolding** (~20 min) — fix
     duplicate signup + capacity bugs, add `pytest`, write tests,
     run them green. Demos agent loop + terminal.
  2. **Plan-first unregister feature** (~20 min) — backend route +
     frontend button + tests, behind an explicit "list files first,
     wait for approval" prompt gate. Demos plan-then-execute pattern.
  3. **Custom chat mode for repeatable scaffolding** (~15 min) —
     learner authors `endpoint-scaffolder.chatmode.md` with pinned
     conventions, then scaffolds a `categories` endpoint from one
     line.
  - Plus stretch Exercise 4 (MCP / GitHub MCP server) if time.
- **Prompting patterns (5):** Plan-first; Verify-by-running;
  Constrain-the-blast-radius; Checklist prompts; Show-me-the-diff
  before commit. Each with before/after.
- **Anti-patterns (6):** vague mega-prompt; no verification step;
  approving plan unread; agent invents API/library; endless
  edit-test loop; vague chatmode.
- Raised 6 open questions for Book (chatmode syntax, terminal
  default, MCP availability, plan/approve UX, auto-approve defaults,
  Lab 2 skills-vs-chatmode overlap).

## Outstanding contradictions to resolve before Kaylee writes lab

- River's Exercise 2 ships an "unregister" feature; Zoe's plan flags
  this as already in Lab 1 (Q1) and proposes alternates. Pick one.
- River's chatmode file uses old path/extension; Book confirms
  current path is `.github/agents/<name>.agent.md`. Update spec
  before lab authoring.

## Notes

- River's design is the strongest source for actual minute-by-minute
  hands-on flow. Kaylee should treat it as the structural baseline
  and overlay Zoe's 3-layer arc + Book's accuracy corrections.
